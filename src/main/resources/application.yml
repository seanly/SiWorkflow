spring:
  application:
    name: SiWorkflow
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/xgdash?useUnicode=true&characterEncoding=utf-8
    username: root
    password: root
  jpa:
    show-sql: true

server:
  port: 9000

logging:
  file: siworkflow.log